\documentclass[../main.tex]{subfiles}
\graphicspath{{\subfix{}}}

% Resolves datapath when this file is being rendered alone
\ifdefined\subfilesmaindocument\else
  \renewcommand{\datapath}{../../output/train/data}
  
  	% Preview plot dimensions
  	\def\plotlarge{}  % comment out for small
	\ifdefined\plotlarge
  		\newcommand{\plotxscale}{1.8}
  		\newcommand{\plotyscale}{0.6}
	\else
  		\newcommand{\plotxscale}{0.9}
  		\newcommand{\plotyscale}{0.5}
	\fi
\fi

\begin{document}

\begin{tikzpicture}
  \begin{axis}
  [			% Plot Settings
    width=\plotxscale\linewidth,
  	height=\plotyscale\linewidth,
    xlabel={Epoch},
    ylabel={Training Time (s)},
    xmin=-5, xmax=215,
    ymin=285, ymax=315,
    grid=minor,
    %legend pos=south east,
  ]				% Trace Settings
	\addplot[
  		%mark = *,
  		thin,
  		blue,
		]		% Trace Source 
		table[
  		col sep=comma,
  		header=has colnames,
  		x expr=\thisrow{epoch},
  		y={epoch_seconds}
		] {\datapath/obSTAtten_256_lif_cifar10_timing.csv};\addlegendentry{obSTAtten};
	\addplot[
  		%mark = *,
  		thin,
  		purple,
		]		% Trace Source 
		table[
  		col sep=comma,
  		header=has colnames,
  		x expr=\thisrow{epoch},
  		y={epoch_seconds}
		] {\datapath/STAtten_256_lif_cifar10_timing.csv};\addlegendentry{STAtten};
	\addplot[
  		%mark = *,
  		thin,
  		violet,
		]		% Trace Source 
		table[
  		col sep=comma,
  		header=has colnames,
  		x expr=\thisrow{epoch},
  		y={epoch_seconds}
		] {\datapath/SDT_256_lif_cifar10_timing.csv};\addlegendentry{SDT};
		
  \end{axis}
\end{tikzpicture}

\end{document}